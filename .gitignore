# ZMK build outputs — produced by build.sh, never committed
*.uf2
build/

# west workspace directories — created by west init/update, never committed
.west/
modules/
zephyr/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDEs / editors
.idea/
.vscode/
.zed/

# direnv
.direnv/
.envrc.local

# Agentic / compound-engineering working artifacts — process notes, not project docs
docs/superpowers/
.compound-engineering/
